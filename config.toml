baseURL = 'https://jkirk.github.io/'
languageCode = 'en-us'
title = 'Share Your Knowledge'
theme = 'binario'

[Author] # Used in authorbox
  name = "Darshaka Pathirana"

[Params]
  description = "Problems need to be solved. Solutions need to be shared." # Site Description. Used in meta description
  schema = true # Enable Schema
  columns = 1 # Set the number of cards columns. Possible values: 1, 2, 3
  dateFormat = "2006-01-02" # Change the format of dates
  authorbox = true # Show authorbox at bottom of single pages if true
  related = true # Enable Related content for single pages
  relatedMax = 5 # Set the maximum number of elements that can be displayed in related block. Optional
  hideNoPostsWarning = false # Don't show no posts empty state warning in main page, if true

[Params.Entry]
  meta = ["date", "categories", "tags"]
  toc = true # Enable Table of Contents
  tocOpen = true # Open Table of Contents block. Optional

[Params.Breadcrumb]
  enable = true # Enable breadcrumb block globally
  homeText = "Main" # Home node text

[Params.Social]
  email = "dpat@syn-net.org"
  github = "jkirk"
